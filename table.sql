CREATE TABLE testtable (
    name TEXT NOT NULL,
    exam_day INTEGER NOT NULL,
    nLang INTEGER,
    math INTEGER,
    Eng INTEGER,
    JHist INTEGER,
    wHist INTEGER,
    geo INTEGER,
    phys INTEGER,
    chem INTEGER,
    bio INTEGER,
    ID INTEGER PRIMARY KEY AUTOINCREMENT
);